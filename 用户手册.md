# LearnPath 用户手册

> 版本：1.0 | 适用平台：Windows / macOS / Linux

---

## 一、项目简介

**LearnPath** 是一款基于 AI 的个性化学习路径规划工具。你只需告诉它你想学什么，它会通过多轮对话了解你的基础、时间、目标和偏好，然后借助 DeepSeek 大模型为你生成 4-8 周的详细学习计划，并自动搜索 B 站高播放量课程作为配套资源。

### 核心功能一览

| 功能 | 说明 |
|------|------|
| AI 学习计划生成 | 多轮对话收集信息，生成结构化周计划 |
| B 站课程推荐 | 按播放量搜索，推荐与每周任务匹配的视频课程 |
| 练习题生成 | 每周围绕学习内容自动生成选择题，支持自定义题型 |
| 错题集 | 答错的题目自动收集，便于复习巩固 |
| 知识摘要 | AI 提炼每周核心概念、关键知识点、常见误区 |
| AI 学习导师 | 随时提问，导师结合你的学习计划进行答疑 |
| 多存档管理 | 支持保存多个学习项目，切换、重命名、删除 |
| Markdown 导出 | 整体计划、单周摘要、练习题均可导出为 Markdown |
| 进度追踪 | 任务勾选、进度条、完成统计 |
| 拖拽排序 | 每周任务支持拖拽调整顺序 |

---

## 二、部署指南

### 2.1 环境要求

- **Python** 3.9+
- **pip**（Python 包管理器）
- 网络畅通（需要访问 DeepSeek API 和 B 站 API）

### 2.2 获取代码

将项目文件夹复制到你的电脑上，结构如下：

```
项目根目录/
├── backend/
│   ├── app.py              # Flask 主程序
│   ├── requirements.txt    # Python 依赖
│   ├── .env                # 环境变量（密钥，不提交 Git）
│   └── .env.example        # 环境变量模板
├── frontend/
│   └── index.html          # 前端单页面
└── .gitignore
```

### 2.3 配置 API Key

本项目使用 DeepSeek API 生成学习计划和答疑。你需要先获取 API Key：

1. 访问 [platform.deepseek.com](https://platform.deepseek.com) 注册并登录
2. 在 API Keys 页面创建新 Key
3. 打开 `backend/.env` 文件，填入你的 Key：

```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
```

> 如果 `.env` 文件不存在，复制 `.env.example` 为 `.env` 后编辑即可。

### 2.4 安装依赖

打开终端，进入 `backend` 目录，执行：

```bash
pip install -r requirements.txt
```

依赖项说明：

| 包 | 用途 |
|---|------|
| flask | Web 框架 |
| flask-cors | 跨域请求支持 |
| requests | HTTP 请求（调用 DeepSeek / B 站 API） |
| python-dotenv | 从 `.env` 文件加载环境变量 |

### 2.5 启动服务

在 `backend` 目录下执行：

```bash
python app.py
```

看到以下输出表示启动成功：

```
LearnPath backend starting...
Visit: http://localhost:5000
```

### 2.6 访问应用

打开浏览器，访问 **http://localhost:5000** 即可使用。

> 注意：前端默认连接 `localhost:5000`。如果部署到服务器，需修改 `frontend/index.html` 中第 745 行的 `API_BASE` 变量为实际地址。

---

## 三、使用教程

### 3.1 界面布局

```
┌──────────┬────────────────────────────────┬──────────┐
│  侧边栏   │          主内容区域              │  AI 对话  │
│          │                                │          │
│ · 总览    │  统计卡片 / 进度条              │  聊天窗口 │
│ · 周列表  │  每周任务 · 课程推荐            │  快捷回复 │
│ · AI对话  │  练习题 · 知识摘要              │  输入框   │
│ · 错题集  │                                │          │
│ · 存档   │                                │          │
└──────────┴────────────────────────────────┴──────────┘
```

- **左侧边栏**：导航菜单、周计划快速跳转、错题列表、存档管理
- **中间主区域**：学习仪表盘，展示统计数据、每周任务和课程推荐
- **右侧面板**：AI 对话窗口，也是创建学习计划的入口

### 3.2 创建学习计划

1. 在右侧对话面板的输入框中，输入你想学习的技能，如 `Python 爬虫`、`机器学习入门`、`日语N3`
2. AI 会依次问你 4 个问题：
   - **当前基础**：零基础 / 有基础 / 中级 / 高级
   - **每周时间**：<5小时 / 5-10小时 / 10-20小时 / 20小时以上
   - **学习目标**：入门了解 / 独立项目 / 求职水平 / 考证
   - **学习偏好**：看视频 / 读文档 / 做项目 / 都可以
3. 回答完毕后，AI 自动生成 4-8 周的学习计划，主区域即时显示

> 每个问题都有快捷选项按钮，点击即可选择，也可以手动输入文字。

### 3.3 查看和管理学习计划

计划生成后，主区域显示：

- **顶部统计卡片**：总周数、任务完成数、课程资源数、每周学习时间
- **总体进度条**：任务完成百分比，每个里程碑对应一周
- **每周详情卡片**：
  - 周标题和任务列表（可勾选完成、可拖拽排序）
  - B 站高播放量课程推荐卡片（显示封面、UP主、播放量、时长）
  - 练习题区域
  - 知识摘要区域

#### 任务操作
- 勾选复选框标记任务完成
- 拖拽任务左侧的 `⋮⋮` 手柄调整顺序

### 3.4 练习题

每周围绕学习任务自动生成选择题：

1. 点击「生成练习题」按钮，AI 自动生成 5 道单选题
2. 选择答案后立即显示对错，正确答案以绿色高亮
3. **重新生成**时可自定义题型，例如：
   - "多出几道代码题"
   - "出 10 道题"
   - "改成判断题"
   - "侧重异常处理相关知识点"
4. 错题自动进入「错题集」（侧边栏可见）
5. 每道题下方有「问下导师」按钮，点击直接向 AI 导师提问
6. 支持导出试题为 Markdown 文件

### 3.5 知识摘要

1. 点击每周的「生成知识摘要」按钮
2. AI 生成该周内容的详细摘要，包含：
   - **核心概念**：3-5 个重要概念，附带简要解释和代码示例
   - **关键知识点**：5-8 个必须掌握的知识点，每个附带可运行代码
   - **常见误区**：初学者易犯错误，附错误 vs 正确代码对比
   - **进阶提示**：2-3 条深入学习的建议和代码演示
   - **推荐练习**：2-3 个具体练习思路和代码框架
3. 支持导出摘要为 Markdown 文件

### 3.6 AI 学习导师

计划生成后，右侧面板自动切换为「AI 学习导师」模式：

- 直接输入学习问题，AI 结合你的背景和当前学习计划进行答疑
- 支持概念讲解、代码调试、学习方法建议等
- 快捷按钮：保存并新建、推荐更多资源、调整计划、提问

### 3.7 调整学习计划

在对话窗口输入调整需求，例如：
- "第一周任务太多了，减少一些"
- "增加一些关于数据库的内容"
- "我觉得难度太低，整体提升一档"

AI 会保持计划结构重新生成。

### 3.8 管理学习存档

- **保存**：在对话中发送「保存并新建学习项目」，或点击侧边栏同名菜单，当前进度自动存档
- **切换**：点击侧边栏「我的存档」下的条目
- **重命名**：悬停在存档上，点击 ✎ 图标，输入新名称后回车
- **删除**：点击存档右侧 × 按钮（会弹出确认框）

### 3.9 错题集

- 做练习题答错的题目自动汇入错题集
- 侧边栏「错题集」区域实时显示数量
- 点击错题可滚动定位到对应周次的练习题
- 点击侧边栏「错题集」菜单进入集中复习视图，所有错题按周分组展示
- 每道错题标注正确/错误选项，可咨询导师或移除

### 3.10 导出功能

| 导出项 | 内容 | 操作位置 |
|--------|------|----------|
| 完整学习计划 | 全部周计划、任务、课程链接、摘要、练习 | 顶部「📥 导出」按钮 |
| 单周试题 | 该周选择题及答案 | 周卡片练习题区域「📥 导出试题」 |
| 单周摘要 | 该周知识摘要 + 任务列表 | 周卡片摘要区域「📥 导出」 |

所有导出格式为 Markdown（`.md`），可直接用 Typora、VS Code 等编辑器打开。

### 3.11 刷新恢复

刷新页面后，应用自动从浏览器本地存储恢复：
- 当前学习计划和进度
- 全部存档
- 错题集
- 同时向后端同步会话状态，确保答疑等功能正常

---

## 四、常见问题

**Q: 启动报错 "DEEPSEEK_API_KEY environment variable is not set"**
A: 请检查 `backend/.env` 文件是否存在且包含正确的 API Key。Key 不能为空。

**Q: 发送消息后显示"无法连接到后端服务"**
A: 确认 Flask 已启动（终端显示 `Running on http://0.0.0.0:5000`）。如果使用了代理，检查代理设置。

**Q: 生成计划很慢**
A: DeepSeek API 调用约需 10-30 秒，请耐心等待。如果超时，检查网络和 API Key 是否有效。

**Q: B 站课程链接打不开**
A: 部分 B 站 API 返回的链接可能需要手动修正。可以点击课程卡片底部的「前往B站观看」，或复制标题到 B 站搜索。

**Q: 如何修改端口号？**
A: 编辑 `backend/app.py` 最后一行 `app.run(host='0.0.0.0', port=5000, ...)` 中的 `port` 值。

**Q: 数据存在哪里？**
A: 所有数据存储在浏览器的 `localStorage` 中，不会上传到任何服务器。清除浏览器数据会导致存档丢失，建议定期导出备份。

---

## 五、项目架构简述

```
用户浏览器                  Flask 后端                  第三方服务
   │                         │                          │
   ├── index.html            ├── app.py                 │
   │   (SPA 单页面)          │   /api/chat              ├── DeepSeek API
   │                         │   /api/search-bilibili   │   (AI 文本生成)
   │   localStorage          │   /api/generate-quiz     │
   │   (数据持久化)           │   /api/generate-summary  ├── B站 API
   │                         │   /api/restore           │   (视频搜索)
   └─────────────────────────┴──────────────────────────┘
```

- **前端**：纯 HTML/CSS/JS，无框架依赖，单页面应用
- **后端**：Flask RESTful API，状态机驱动对话流程
- **AI**：DeepSeek Chat 模型，处理计划生成、答疑、出题、摘要
- **存储**：前端 localStorage（用户侧），后端 session 字典（服务端，重启丢失）

---

> 有任何问题或建议，欢迎反馈。
